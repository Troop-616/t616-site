[project]
name = "t616-site"
version = "0.1.0"
description = "Static site compiler and TroopWebHost sync utilities for Boy Scout Troop 616"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pyyaml>=6.0",
    "beautifulsoup4>=4.12.0",
    "playwright>=1.40.0",
]

